Change env.txt to .env and fill the required credentials

Run docker compose up --build to start the backend application

Server_Base_Url : http://localhost:8081/

Google Oauth Signin : http://localhost:8081/oauth2/authorization/google
